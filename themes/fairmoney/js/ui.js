/*--------------------------------------------------------------
Document Ready For UI and Animation - Ev to merge
--------------------------------------------------------------*/

jQuery(function(){
/*
  jQuery(window).keydown(function(event){
    var isNotValid = false;

    jQuery.listen('parsley:field:error', function(){
      console.log('error');
        isNotValid = true;
    });

    if(event.keyCode === 13 && isNotValid === true) {
      event.preventDefault();
      console.log(isNotValid);
      return false;
    }
  });
*/
});